import 'package:flutter/material.dart';

// 🔑 This key allows us to navigate from anywhere in the app (even outside widgets)
final GlobalKey<NavigatorState> navigatorKey = GlobalKey<NavigatorState>();